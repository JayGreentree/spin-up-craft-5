{% extends "_layouts/_layout" %}

{% block content %}
<div class="jumbotron">
    <h1 class="text-center">{{ entry.title }}</h1>
</div>

<div class="container">
<div class="row">

{{ entry.pageContent }}
</div>
</div>
{% endblock %}
